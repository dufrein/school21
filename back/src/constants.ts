// эндпоинты должны начинаться с /api, потому что это адрес апишки для фронта
export enum ApiEndpoints {
  Users = "/api/users",
  Tariffs = "/api/tariffs",
  Courses = "/api/courses",
  Lessons = "/api/lessons", 
}

export const maxWeightImgageMb = 10;
